abstract: 'Replace this placeholder with the abstract. Keeping it in a separate metadata file lets the pipeline regenerate it (e.g., to inject computed numbers) without touching the manuscript source.'
